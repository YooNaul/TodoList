<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.todolist.mapper.MemberMapper">

	<!-- 로그인 -->
    <select id="memberLogin" resultType="com.todolist.model.MemberVO">
    
       select id, name, sex, ranking, pass from todo_member where ID = #{id} and PASS = #{pass}
    
    </select>
    
    <!-- 아이디 중복검사 -->
  	<select id="idCheck" resultType="int">
  	
  		select count(*) from todo_member where ID = #{memberId}
  	
  	</select>
  	
  	<insert id="memberJoin">
  	
  		insert into todo_member (id, name, pass, sex, email, status)
  			values (#{id}, #{name}, #{pass}, #{sex}, #{email}, 2)
  	
  	</insert>

</mapper>